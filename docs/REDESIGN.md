# Fresh edition — current local implementation

The fresh edition now occupies `/`. It replaces the rejected object-based composition with distinct abstract prints, texture, a translucent discovery layer and minimal incident linework. Movement differs per scene; reading holds precede complete comparisons in document flow. Current implementation and asset paths are in README.md and the latest docs/CONTEXT.md entry. The user has not yet reviewed this edition. No publication is authorised.

## Historical rejected implementation (not the current design)

Status: **rejected by the user; redesign incomplete**. The prior reviewer’s ship disposition did not establish visual success. Object selection, composition, overlap and thread attachment were rejected. `/composition/` is an unapproved connected-field experiment, not the whole-site replacement. No push or deployment authorised.

## Direction

The user selected Crossing, then expanded the prototype to the entire site: beauty and subtlety take precedence over easy modification. Ivory, charcoal engraving and rust italic typography inherit their LinkedIn artwork. Text occupies open space in the scene; never place an opaque or translucent panel behind an argument. Abstract architecture is conceptual, not documentary evidence.

## Information architecture

Six independent questions, not predicted events: surveillance/release (who may act), defense/discovery (who benefits), incident/coordination (who answers). Stable anchors, complete essay, original sources, objections, six obligations and both incident origins remain. Evidence follows each situation and comparison in native disclosures.

Ten comparison sequences share ScrollComparison and Argument components. Natural document scroll dwells on the first perspective, gradually crosses separate architectural foreground and middle layers, dwells on the second (and third), then makes every argument available together. Two-way comparisons settle in the scene. Three-way comparisons open into normal-flow, unbounded columns below it. There are no nested reading scrollers and no wheel interception.

## Composition and motion

Separate transparent cutouts carry the six metaphors: surveillance screen, release threshold, defensive shelter fins, discovery's exploded structure, incident fracture, and coordination's balanced sails. A low distant horizon and SVG rust threads form independent depth planes. Chapter entrances use their own engraved landscapes and foreground cutouts. Typography is semantic Instrument Serif with rust italic emphasis and DM Sans body text, self-hosted with licences.

Desktop comparison enhancement requires width over 1000px, height at least 800px, and measured room for the entire focused argument. Smoothstep scroll intervals provide holds and gradual crossings over 400svh (two) or 480svh (three), without timers or scroll capture. Read together bypasses a scene. Mobile, short screens, reduced motion, manual pause and no JavaScript retain all text in static document flow.

## Assets and verification

New `public/assets/abstract/crossing-*-v2.webp` assets have prompt/provenance sidecars; cutout alpha was checked. `social-crossing-v2.jpg` is 1200 × 630 with a dedicated full prompt. Earlier assets remain for provenance.

Ten Playwright checks pass against preview: complete authored arguments without JavaScript, original external-link baseline, full essay, reversible two/three-way scene progress, read-together keyboard bypass, incident origin evidence, navigation, motion preferences, short/mobile layouts, no nested scrollers, local assets and social metadata. Browser screenshots are in `.impeccable/world-review/`. Detector advisories concern mask alpha black and intentional type sizes against the superseded design document, not new visible palette colours.

Print, Safari and a full assistive-technology audit are not covered by these checks. Infrastructure is unchanged. Final design rules are recorded in DESIGN.md and its sidecar after fresh review.

Production nginx container build, HTTP smoke checks and all ten browser checks passed. Formatting and diff whitespace checks passed. Fresh review used a default agent with the supplied Impeccable reviewer procedure because the named role was unavailable. Its one material finding, hero attribution contrast, was resolved by moving the credit into adjacent plain reading flow; desktop/mobile recaptures confirmed the fix. Final verdict: **ship**, no open material findings. No independent saved seed-roll output exists; the direction seed is retained in the emitted contract and was reported by session context.
