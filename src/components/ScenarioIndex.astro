---
import { scenarios } from "../data/scenarios.js";
const groups = [
  { title: "Who may act?", ids: ["surveillance", "release"] },
  { title: "Who benefits?", ids: ["defense", "discovery"] },
  { title: "Who answers?", ids: ["incident", "race"] },
];
---

<section
  id="decisions"
  class="scenario-index"
  aria-labelledby="scenario-index-title"
>
  <h2 id="scenario-index-title">
    Six situations.<br /><em>Questions we share.</em>
  </h2>
  <p>
    Start anywhere, or read in order. These are independent situations—not steps
    in a predicted future. Each returns to the responsibilities that could
    govern it.
  </p>
  <div class="scenario-groups">
    {
      groups.map((group) => (
        <div class="scenario-group">
          <>
            <h3>{group.title}</h3>
            <div class="scenario-index-grid">
              {group.ids.map((id) => {
                const s = scenarios.find((s) => s.id === id);
                return (
                  <a href={`#${s.id}`}>
                    <h4>
                      {s.number} / {s.title}
                    </h4>
                    <p>{s.question}</p>
                  </a>
                );
              })}
            </div>
          </>
        </div>
      ))
    }
  </div>
</section>
