---
import { scenarios } from "../data/scenarios.js";
---

<nav class="chapter-nav" aria-label="Chapters">
  <a href="#decisions">All scenarios</a>{
    scenarios.map((s) => (
      <a href={`#${s.id}`}>
        <span>{s.number}</span>
        {s.title}
      </a>
    ))
  }<a href="#obligations">Common ground</a>
</nav>
